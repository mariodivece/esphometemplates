binary_sensor:
  - platform: gpio
    pin:
      number: GPIO07
      mode:
        input: true
        pulldown: true
    filters:
      - delayed_on: 5s
    on_press:
      then:
        - button.press: button_restart
